任务：把 00_收件箱\待归档\ 里的文件正式归档进库。
这是 C 档任务，先按 AGENTS.md 第 0 节出一份完整计划（7 项）等我批准。

工具（**不要另写抽取逻辑**）：
    python _tools\kb_import.py --cat 10_基金本子 <文件路径...>
先用 --dry-run 预演，把编号分配结果发我看；我批准后再去掉 --dry-run 真跑。

入库后：
  1) python _tools\kb_rebuild_index.py        （同步 INDEX/TAGS）
  2) 更新 CATALOG.md、PROGRESS.md、_log\CHANGELOG.md

入库后原件的去向（工具自动处理，不用你手动移）：
  - 成功入库 -> 原件自动移进 `00_收件箱\已归档\`
  - 指纹重复 -> 原件自动移进 `00_收件箱\重复文件\`
  - 需人工确认 -> 原件自动移进 `00_收件箱\需人工确认\`
  收件箱来源的文件是**移动**，素材区等库外来源是**复制**（素材区永远不动）。
  所以跑完之后 `待归档\` 应当是空的。

注意：单批超过 30 篇请拆批；分类不确定时先问我，不要瞎归。