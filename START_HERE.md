# 从这里开始（给我自己看的）

> 这一页是**给你自己用的导航**，不用记任何命令。想干什么，找到那一行就行。

---

## 一、我最常做的 4 件事

### 1️⃣ 我想看库里有什么本子

- 打开 **Obsidian** → 左边点 `10_基金本子` → 点 **`INDEX.md`**（一张总表）
- 想按方向找：`Ctrl + Shift + F` 搜关键词
- 标着「可借鉴度 = 高」的，是最值得学的

### 2️⃣ 我要往库里加新文件

1. 把文件**拖进** `D:\KnowledgeBase\00_收件箱\待归档\`
2. 打开任意一个 agent，说一句：**「归档收件箱」**
3. 它会先给你一份计划，你看懂了就说「**批准**」

> 什么文件都能收：PDF、Word、PPT、Excel、网页、纯文本。

### 3️⃣ 我要写本子，想找参照

打开 agent，说：

> **我要写 XX 方向的青年基金，库里哪些本子能当参照**

（完整说法见 `_prompts\04_找参照.md`）

### 4️⃣ 我想确认库还好不好

库里同一份文件如果有多版（比如制度的每年修订），**每个版本都单独建了卡片**，
而且卡片之间用 **Obsidian 链接**连着：

- 打开新版卡片，里面有一行 `[[旧版编号]]`，**用鼠标点一下就跳过去**
- 打开旧版卡片，同样有一行指向新版
- 每张卡片里还有一节「**版本关系**」，写清哪个是现行、哪个已被取代
- **要对比两版差异**：两个版本的原始 PDF 都在 `raw\` 里，点开对着看

> 这就是为什么库用 Obsidian 当界面 —— 普通记事本打开 `[[xxx]]` 只是一串文字，
> Obsidian 里它是能点的链接。

### 5️⃣ 我想查某个旧版本

说：**「按 08_体检.md 跑一次体检」**

---

## 二、库的结构（一眼看懂）

| 文件夹 | 放什么 |
|---|---|
| `10_基金本子` | 中标的基金申请书（本子） |
| `20_教学` | 课件、教案、试卷 |
| `30_办公` | 通知、报告、表格 |
| `40_文献` | 综述、标准、专利、教材等参考资料 |
| `45_论文` | **学术论文**（写本子时引用的弹药库） |
| `50_模板库` | 以后从真实卡片里提炼的模板（现在还空着） |
| `00_收件箱` | 新文件先扔这，等归档 |
| `_schema` | 卡片格式规范 |
| `_tools` | 抽取工具（agent 用，你不用管） |
| `_prompts` | **给 agent 的提示词**（复制粘贴用） |
| `_log` | 变更日志、交接单、备份规程 |

### 每个分类里面的结构（以基金本子为例）

```
10_基金本子\
  raw\      原件（PDF/Word）—— 只读，永远别动
  md\       抽取出来的文字 —— agent 读这个
  cards\    ★卡片 —— 一页看懂一篇本子，你主要看这个
  INDEX.md  总表（哪篇是什么方向、可借鉴度多高）
  TAGS.md   按标签找（比如搜"技术路线图"）
```

---

## 三、四个 agent 怎么用

| Agent | 怎么开始 |
|---|---|
| **harness** / **Codex** / **Claude Code** | 在 `D:\KnowledgeBase` 目录里打开，直接说需求 |
| **Kimi** | ⚠️ **必须先粘一段开场白**，见 `_prompts\10_一句话接入.md` |

---

## 四、几条规矩（重要，别踩）

1. **`raw\` 里的原件永远别动** —— 那是底稿，动了卡片就对不上了
2. **卡片和 INDEX 别手工改** —— 由 agent 维护，手改会和索引对不上
3. **库里有别人没公开的本子**：不外传、不传到网上、不生成分享链接
4. agent 做任何写入前都会**先给你计划**，看不懂就让它用大白话解释，别怕问

---

## 五、换电脑 / 多台电脑

见 `_log\BACKUP.md`。核心就一条：**同一时间只在一台电脑上写**。

---

## 六、卡住了怎么办

| 现象 | 怎么办 |
|---|---|
| agent 弹审批框 | 点「**允许**」；不点它就会一直等 |
| 索引和卡片对不上 | 让 agent 跑 `python _tools\kb_rebuild_index.py` |
| 抽取出来是乱码 | 让 agent 检查编码（`.doc` 要用本机 Word） |
| 不知道某个文件在哪 | 让 agent 用 grep 搜，别自己翻 |

## 七、找我正在看的这份说明书的详细版

`60_使用手册\` 里有**完整的手册**，按需要翻：

| 我想…… | 翻这一章 |
|---|---|
| 五分钟搞懂最常用的操作 | `60_使用手册\01_五分钟上手.md` |
| 学 Obsidian（搜索、双链、看原件） | `60_使用手册\02_Obsidian使用指南.md` |
| 往库里加文件 | `60_使用手册\04_添加新文件.md` |
| 写本子时用库里的素材 | `60_使用手册\05_用库写本子.md` |
| 备份 / 换电脑 / 多机同步 | `60_使用手册\06_备份与迁移.md` |
| 出问题了 | `60_使用手册\08_常见问题.md` |
| 看不懂某个词 | `60_使用手册\10_术语表.md` |

---

## 八、搜索技巧（省得翻半天）

### 先做一次设置，一劳永逸

`Ctrl + Shift + F` 搜的是**全库**，会连 `md\`（抽取的原文全文）和 `INDEX.md` 一起搜，
所以结果常常多得看不过来。把这些排除掉，搜索就干净了：

**设置 → 文件与链接 → 排除的文件**，加入：

```
INDEX.md
TAGS.md
10_基金本子/md
20_教学/md
30_办公/md
45_论文/md
```

### 临时缩小范围

| 想干什么 | 搜索框里输入 |
|---|---|
| 只在办公分类里搜 | `path:30_办公 评价体系` |
| 按标签搜 | `tag:#制度` |
| 排除原文，只搜卡片 | `评价体系 -path:md` |
| 排除索引文件 | `评价体系 -file:INDEX -file:TAGS` |

**最直观的一招**：左侧文件树里**右键点分类文件夹** → 「**在文件夹中搜索**」。

### 另一个快捷键

`Ctrl + O` —— 快速跳转，输几个字模糊匹配文件名，回车直接打开。

---

## 八、卡片改名 / 双链点不动了怎么办

库里卡片文件名是 `编号 短标题`（例：`2025-制度-0002 大连理工大学专任教师学术贡献评价体系（2025年4月修订）`），
卡片里靠 YAML 的 `aliases` 字段保住 `[[编号]]` 这种短链接。

**如果 agent 批量改了卡片或文件名之后，发现双链点不动了**：

> **完全退出 Obsidian，再重新打开**（不是关窗口，是退出程序）。

Obsidian 靠文件监听感知变化，外部程序（agent）批量改文件时它常常漏掉。
重启后会重新索引，链接就恢复了。**这不是库坏了，只是界面没刷新。**

另外注意：**YAML 属性区里的链接不会渲染成可点的**，
能点的是**正文里**的链接（比如制度卡片正文的「版本关系」那节）。
